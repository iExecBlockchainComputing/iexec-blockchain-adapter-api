package com.iexec.blockchain.command.task.finalize;

import com.iexec.blockchain.command.generic.CommandRepository;

public interface TaskFinalizeRepository extends CommandRepository<TaskFinalize> {
}
